const Shift = require("./shift.model.js");
var express = require('express');
var router = express.Router();
const uuidv4 = require('uuid/v4');

/*
Replaced by shift.controller.js

General requests can be placer in this file, i.e. request not specific to shifts, personell or other entities (place them in <entity>.controller.js for good structure)
*/


module.exports = router;
